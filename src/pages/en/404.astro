---
import Layout from '../../layouts/Layout.astro';
import NotFound from '../../components/sections/NotFound.astro';
import { t } from '../../i18n/ui';

const ui = t('en');
---

<Layout title={`404 — ${ui.notFound.title}`} description={ui.notFound.message} noindex>
	<NotFound />
</Layout>
